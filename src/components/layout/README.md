# Layout components

Structural, page level pieces that make up the shared site shell.

- `MainLayout.jsx` wraps every route with the announcement bar, header,
  and footer.
- `AnnouncementBar.jsx` the scrolling strip above the header.
- `Header.jsx` site navigation.
- `Footer.jsx` simple footer, expanded in a later phase.
