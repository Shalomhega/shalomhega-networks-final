# Common components

Shared, cross page pieces that are specific to SHALOMHEGA NETWORKS content:
things like a service card, a testimonial block, a section heading pattern.
Not generic UI primitives, those belong in `ui`.

Nothing here yet. This folder is prepared for a future phase.
